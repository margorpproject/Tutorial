### 1. Convert byte array into string
> str = bytes.decode("utf-8")

### 2. Split string into a string array
> ary = "1,2,3,4,5".split(",")

### 3. Join elements of array into a string
> str = ",".join([1,2,3,4,5])
